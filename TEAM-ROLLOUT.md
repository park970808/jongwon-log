# 팀별 활동일지 전환

기존 장원급제 자료는 이미 `teams/jangwon/…` 경로에 있습니다. 옮기거나 지우지 마세요. `/`는 기존 팀을 계속 열고, `?team=team-2`는 새 팀을 엽니다. 같은 GitHub Pages 사이트와 Firebase 프로젝트를 사용합니다.

## 실서비스 적용 전에

1. Firebase Console의 Firestore **현재 규칙을 백업**하고, `firestore.rules`와 다른 서비스가 공유하는 규칙이 있는지 비교하세요. 이 파일은 앱이 사용하는 경로만 허용하며 그 외 경로는 거부합니다.
2. Firestore에 관리자 권한으로 `teams/jangwon` 문서(`name: "장원급제"`)와 `teams/team-2` 문서(`name: "2팀"` 또는 실제 팀명)를 만드세요. 기존 하위 컬렉션은 그대로 둡니다.
3. `teamInvites/jangwon`, `teamInvites/team-2` 문서를 각각 만들고, 각 문서의 `code` 필드에 **서로 다른 길고 무작위인 초대 코드**를 넣으세요. 이 코드는 코드 저장소나 URL에 넣지 않고 해당 팀 사용자에게만 전달합니다. Firestore 콘솔에서 만드는 작업은 클라이언트 보안 규칙의 제한을 받지 않습니다.
4. 짧은 점검 시간에 규칙 시뮬레이터 또는 에뮬레이터로 팀 A 가입·읽기, 팀 B 가입·읽기, 타 팀 읽기 거부, 잘못된 코드 거부를 검증하세요. 기존 규칙에 다른 앱 경로가 있다면 병합해야 합니다.
5. **규칙을 먼저 배포**한 다음 새 웹 파일(`index.html`, `team-routing.mjs`, `template.hwpx`)을 GitHub Pages에 함께 배포하세요. 사이에 잠시 앱 접근이 중단될 수 있으니 사용 중인 사람이 없을 때 진행하세요. 규칙만 배포한 채 오래 두지 마세요.
6. 기존 팀은 원래 주소에서, 새 팀은 `?team=team-2` 주소에서 초대 코드를 한 번 입력합니다. 익명 인증의 브라우저 저장소를 지우거나 다른 기기를 쓰면 다시 가입해야 합니다. 두 팀의 읽기/쓰기와 HWPX 내보내기를 각각 확인하세요.

`teamId`는 저장 경로일 뿐 권한이 아닙니다. `firestore.rules`를 배포하지 않은 상태에서는 팀 주소를 아는 사용자가 다른 팀 자료를 읽을 수 있으므로 새 팀에 실제 자료를 입력하지 마세요. 팀 멤버끼리는 기존 앱과 마찬가지로 공동 편집하며, 자리 선택은 팀 내부의 보안 권한이 아닙니다.
